<div class="container">
  <div class="row justify-content-center">
    <div class="col-lg-8">
      <div class="card shadow-sm">
        <div class="card-body">
          <h1 class="card-title h3 mb-4 fw-bold"><%= title %></h1>

          <%= render "form", result: result, cancel_path: cancel_path, submit_text: submit_text %>
        </div>
      </div>
    </div>
  </div>
</div>
