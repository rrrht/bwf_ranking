<!DOCTYPE html>
<html>
  <head>
    <title>BwfRanking</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <!-- Bootstrap 5 -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous">
  </head>

  <body class="bg-light">
    <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm py-3 mb-4">
      <div class="container">
        <a class="navbar-brand fw-bold" href="<%= root_path %>">バドミントン世界ランキング</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav me-auto">
            <li class="nav-item">
              <%= link_to "ランキング", root_path, class: "nav-link #{current_page?(root_path) ? 'active fw-semibold' : ''}" %>
            </li>
            <li class="nav-item">
              <%= link_to "選手", players_path, class: "nav-link #{current_page?(players_path) ? 'active fw-semibold' : ''}" %>
            </li>
            <li class="nav-item">
              <%= link_to "トーナメント", tournaments_path, class: "nav-link #{current_page?(tournaments_path) ? 'active fw-semibold' : ''}" %>
            </li>
            <li class="nav-item">
              <%= link_to "試合結果", results_path, class: "nav-link #{current_page?(results_path) ? 'active fw-semibold' : ''}" %>
            </li>
          </ul>

          <div class="d-flex align-items-center">
            <% if admin_user_signed_in? %>
              <span class="me-3 text-success small"><i class="bi bi-person-circle me-1"></i><%= current_admin_user.email %></span>
              <%= link_to "Logout", destroy_admin_user_session_path, method: :delete, class: "btn btn-outline-danger btn-sm" %>
            <% else %>
              <%= link_to "Admin Login", new_admin_user_session_path, class: "btn btn-outline-primary btn-sm" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <main class="container">
      <%= render "shared/flash" %>
      <%= yield %>
    </main>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js"
      integrity="sha256-3gQJhtmj7YnV1fmtbVcnAV6eI4ws0Tr48bVZCThtCGQ="
      crossorigin="anonymous"></script>

    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </body>
</html>
