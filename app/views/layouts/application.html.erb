<!DOCTYPE html>
<html>
  <head>
    <title>BwfRanking</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <!-- Bootstrap CDN -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous">
  </head>

  <body class="bg-light p-4">
    <nav class="navbar navbar-expand-lg navbar-light bg-light mb-4">
      <div class="container-fluid">
        <a class="navbar-brand fw-bold" href="<%= root_path %>">🏸 BWF Ranking</a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <%= link_to "ランキング", root_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "選手一覧", players_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "大会一覧", tournaments_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "試合結果一覧", results_path, class: "nav-link" %>
            </li>
          </ul>

          <div class="d-flex align-items-center ms-auto">
            <% if admin_user_signed_in? %>
              <span class="text-success me-2"><%= current_admin_user.email %></span>
              <%= link_to "Logout", destroy_admin_user_session_path, method: :delete, class: "btn btn-sm btn-outline-danger" %>
            <% else %>
              <%= link_to "Admin Login", new_admin_user_session_path, class: "btn btn-sm btn-outline-primary" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <%= yield %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js"
      integrity="sha256-3gQJhtmj7YnV1fmtbVcnAV6eI4ws0Tr48bVZCThtCGQ="
      crossorigin="anonymous"></script>

    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </body>

</html>
